import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#F5FDFF",
    width: "100%",
    borderRadius: 20,
    flexDirection: "row",
    paddingVertical: 10,
  },
  leftContainer: {},
});